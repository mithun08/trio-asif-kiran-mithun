entries:
  - role_id: "EVAL-01"
    kind: exact
    description: "Senior Python engineer — expect Python-skilled candidate in top 3"
    expected_top_emails:
      - "alice.test@example.com"

  - role_id: "EVAL-02"
    kind: exact
    description: "Java developer — expect Java-skilled candidate in top 3"
    expected_top_emails:
      - "bob.test@example.com"

  - role_id: "EVAL-05"
    kind: exact
    description: "Python Data Engineer — high-feedback candidate should rank in top 3"
    expected_top_emails:
      - "highfeedback.test@example.com"

  - role_id: "EVAL-02"
    kind: negative
    description: "Java role — Python-only candidate with zero Java overlap should NOT rank in top 3. KNOWN FAILING: ties at skill_match=0 currently break by workbook insertion order, not a principled signal (FR-36 'fewest gaps + bridgeability' ranking is not yet implemented); tracked here deliberately rather than hidden. Fix ranker tie-break to resolve."
    expected_top_emails:
      - "python.only@example.com"

  - role_id: "EVAL-03"
    kind: gap
    description: "C++ role — no consultant has C++/Embedded Systems; candidates must still surface (FR-36) but flagged skill_gap, not silently ranked as if qualified"

  - role_id: "EVAL-04"
    kind: gap
    description: "COBOL mainframe role — no consultant has these skills; a real-but-unsupplied skill must surface ranked candidates flagged skill_gap, not an empty result (FR-36)"
