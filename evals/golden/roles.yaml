entries:
  - role_id: "EVAL-01"
    kind: exact
    description: "Senior Python engineer — expect Python-skilled candidate in top 3"
    expected_top_emails:
      - "alice.test@example.com"

  - role_id: "EVAL-02"
    kind: exact
    description: "Java developer — expect Java-skilled candidate in top 3"
    expected_top_emails:
      - "bob.test@example.com"

  - role_id: "EVAL-05"
    kind: exact
    description: "Python Data Engineer — high-feedback candidate should rank in top 3"
    expected_top_emails:
      - "highfeedback.test@example.com"

  - role_id: "EVAL-02"
    kind: negative
    description: "Java role — Python-Data candidate with no Java skills must NOT appear in top 3"
    expected_top_emails:
      - "highfeedback.test@example.com"

  - role_id: "EVAL-03"
    kind: negative
    description: "C++ role — Python-only consultant with no C++ skills must NOT rank above Java candidate"
    expected_top_emails:
      - "alice.test@example.com"
