from __future__ import annotations

from pathlib import Path


def configure_dspy_cache(cache_dir: Path) -> None:
    """Configure DSPy response caching to the given directory."""
    raise NotImplementedError
